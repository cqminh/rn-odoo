name: Publish

on:
  push:
    tags:
      - 'v*'

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          registry-url: https://registry.npmjs.org

      - run: npm install -g npm@11

      - run: yarn install --immutable

      - run: yarn prepublishOnly

      - run: npm publish --tag ${{ contains(github.ref_name, '-') && 'next' || (startsWith(github.ref_name, 'v1.') && 'v1-legacy' || 'latest') }} --access public --provenance